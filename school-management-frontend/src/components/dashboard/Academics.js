import React from 'react';
import { Typography, Container } from '@mui/material';

const Academics = () => {
    return (
        <Container>
            <Typography variant="h4" gutterBottom>
                Academics
            </Typography>
            {/* Add academic-related content and components */}
        </Container>
    );
};

export default Academics;
