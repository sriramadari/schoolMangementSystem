import React from 'react';
import { Typography, Container } from '@mui/material';

const Grading = () => {
    return (
        <Container>
            <Typography variant="h4" gutterBottom>
                Grading
            </Typography>
            {/* Add grading related content and components */}
        </Container>
    );
};

export default Grading;
