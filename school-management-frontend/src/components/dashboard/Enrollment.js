import React from 'react';
import { Typography, Container } from '@mui/material';

const Enrollment = () => {
    return (
        <Container>
            <Typography variant="h4" gutterBottom>
                Enrollment
            </Typography>
            {/* Add enrollment-related content and components */}
        </Container>
    );
};

export default Enrollment;
