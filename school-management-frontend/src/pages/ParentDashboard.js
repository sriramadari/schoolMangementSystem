import React from 'react';
import { Typography, Container } from '@mui/material';

const ParentDashboard = () => {
    return (
        <Container>
            <Typography variant="h4" gutterBottom>
                Parent Dashboard
            </Typography>
            {/* Add parent dashboard content and components */}
        </Container>
    );
};

export default ParentDashboard;
