import React from 'react';
import { Typography, Container } from '@mui/material';

const StudentDashboard = () => {
    return (
        <Container>
            <Typography variant="h4" gutterBottom>
                Student Dashboard
            </Typography>
            {/* Add student dashboard content and components */}
        </Container>
    );
};

export default StudentDashboard;
