import React from 'react';
import { Typography, Container } from '@mui/material';

const AdminDashboard = () => {
    return (
        <Container>
            <Typography variant="h4" gutterBottom>
                Admin Dashboard
            </Typography>
            {/* Add admin dashboard content and components */}
        </Container>
    );
};

export default AdminDashboard;
